/* Tabuada - 01 ao 10
Versão 1.0
Desenvolvido em linguagem C
Responsável: Renan Gomes
*/

#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <ctype.h>

int main()
{
    setlocale(LC_ALL,"");

    int i = 0, num = 0;
    char c;
    
    printf("Olá, este programa executa a tabuada que você deseja até tabuada do 10. \n\n");
    printf("Qual tabuada você deseja?\n");
    c = getchar();

    while((c == '2')&&(i<=9)){
        i++;
        num++;
        printf("2 x %i = %i\t\n", num, num*2);
    }
    
    while((c == '3')&&(i<=9)){
        i++;
        num++;
        printf("3 x %i = %i\t\n", num, num*3);
    }
    
    while((c == '4')&&(i<=9)){
        i++;
        num++;
        printf("4 x %i = %i\t\n", num, num*4);
    }
    
    while((c == '5')&&(i<=9)){
        i++;
        num++;
        printf("5 x %i = %i\t\n", num, num*5);
    }
    
    while((c == '6')&&(i<=9)){
        i++;
        num++;
        printf("6 x %i = %i\t\n", num, num*6);
    }
    
    while((c == '7')&&(i<=9)){
        i++;
        num++;
        printf("7 x %i = %i\t\n", num, num*7);
    }
    
    while((c == '8')&&(i<=9)){
        i++;
        num++;
        printf("8 x %i = %i\t\n", num, num*8);
    }
    
    while((c == '9')&&(i<=9)){
        i++;
        num++;
        printf("9 x %i = %i\t\n", num, num*9);
    }
    
    while((c == '10')&&(i<=9)){
        i++;
        num++;
        printf("3 x %i = %i\t\n", num, num*10);
    }
    return 0;
}
